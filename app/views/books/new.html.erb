<h1>New books</h1>
 

 <% if @bbbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bbbook.errors.count, "error") %> prohibited this book from being saved:</h2>
      <ul>
        <% @bbbook.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>
 
  <h3>New Books</h3>
   <%= form_with model: @book do |f| %>
    <h4>Title</h4>
     <%= f.text_field :title %>

    <h4>Body</h4>
     <%= f.text_area :body %>

    <div class="c"><%= f.submit 'Create Books' %></div>
   <% end %>
